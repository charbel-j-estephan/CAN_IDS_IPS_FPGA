// Generated by src/export_verilog.py from model.json.
// Do not edit: recalibrate and regenerate.
//
// 2 comparators, 1 tree, 1 of 1 votes to flag.
// Calibrated on 444992 attack-free frames, with
// no attack data: thresholds are what normal traffic does plus a margin.
//
// Every threshold is an integer and every feature is an integer, so these
// comparisons are exactly the ones the Python model scored. Nothing is
// quantised on the way out.
//
// Combinational. The caller supplies the features for one frame and gets
// the verdict in the same cycle; register the output if the fabric needs it.

module can_ids_classifier (
    input  wire [11:0] dt_ratio_q6,
    input  wire [11:0] id_rate,
    output wire        attack
);

    // one comparator per distinct (feature, threshold) pair
    wire le_id_rate_80 = (id_rate <= 12'd80);
    wire le_dt_ratio_q6_39 = (dt_ratio_q6 <= 12'd39);

    // each tree's verdict, as the OR of the paths that reach an ATTACK leaf
    wire tree0 = (le_id_rate_80 && le_dt_ratio_q6_39) || !le_id_rate_80;

    assign attack = tree0;

endmodule
